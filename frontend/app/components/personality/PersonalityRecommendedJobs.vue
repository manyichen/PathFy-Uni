<script setup lang="ts">defineProps<{ jobs: string[]; advice?: string }>()</script>
<template><UCard v-if="jobs.length || advice"><template #header><div><h2 class="font-semibold">岗位探索线索与职业建议</h2><p class="mt-1 text-xs muted">类型资料提供搜索起点，不代表职业适合性结论。</p></div></template><div v-if="jobs.length" class="flex flex-wrap gap-2"><UButton v-for="x in jobs" :key="x" :to="{ path: '/jobs', query: { q: x, from: 'personality' } }" size="sm" color="neutral" variant="soft" trailing-icon="i-lucide-search">{{ x }}</UButton></div><p v-if="advice" class="mt-4 leading-7 muted">{{ advice }}</p></UCard></template>
