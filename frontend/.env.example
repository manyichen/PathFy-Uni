# Nuxt server/build-time configuration. Copy to frontend/.env.

# Browser-visible API base. Leave empty for same-origin /api requests.
NUXT_PUBLIC_API_BASE=

# Development-only Vite proxy target for /api.
FLASK_BACKEND=http://127.0.0.1:5000
